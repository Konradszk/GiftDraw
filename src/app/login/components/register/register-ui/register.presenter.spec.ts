import { RegisterPresenter } from './register.presenter';

describe('Register.Presenter', () => {
  it('should create an instance', () => {
    // expect(new RegisterPresenter()).toBeTruthy();
  });
});
