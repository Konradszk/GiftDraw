import { RegisterContainer } from './register.container';

describe('Register.Container', () => {
  it('should create an instance', () => {
    // expect(new RegisterContainer()).toBeTruthy();
  });
});
