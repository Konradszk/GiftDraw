import { DrawCreateContainer } from './draw-create.container';

describe('DrawCreate.Container', () => {
  it('should create an instance', () => {
    expect(new DrawCreateContainer()).toBeTruthy();
  });
});
