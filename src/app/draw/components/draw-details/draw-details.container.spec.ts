import { DrawDetailsContainer } from './draw-details.container';

describe('DrawDetails.Container', () => {
  it('should create an instance', () => {
    expect(new DrawDetailsContainer()).toBeTruthy();
  });
});
