import { DrawDashboardContainer } from './draw-dashboard.container';

describe('DrawDashboard.Container', () => {
  it('should create an instance', () => {
    expect(new DrawDashboardContainer()).toBeTruthy();
  });
});
